#include <iostream>
#include <stdio.h>
#include <window.h>
using namespace std;

bool numlock_activate = false;

float spd;

void moveMouse(float dx, float dy){
    mouse_event(MOUSEEVENTF_MOVE, dx, dy, 0, 0);
}

int main(){

    while (true){
        
    }
}
